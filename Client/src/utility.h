#ifndef UTILITY_H
#define UTILITY_H

void get_input_line(char* buffer, unsigned int max_len);

// TODO: could do a generic get command function
// enum Command get_command(char* arg0, char* arg1);

#endif
